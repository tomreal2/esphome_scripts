substitutions:
  sonoff_num: "05"
  
esphome:
  name: power-sonoff-${sonoff_num}
  friendly_name: power-sonoff-${sonoff_num}

esp8266:
  board: esp01_1m

# Enable logging
logger:

# Enable Home Assistant API
api:
  encryption:
    key: "s3K4IqwAuyc0yS+OH9okXyr9KbOfrGPE35nBbhxg0vs="

ota:
  - platform: esphome
    password: "c6646c837428043d41dde1b825099638" #"c6646c837428043d41dde1b825099638" #"a4e74f89ca10e62fab316b2b9f937e55"

wifi:
  ssid: !secret wifi_ssid
  password: !secret wifi_password
  manual_ip:
    static_ip: 192.168.1.53        # ← match the reservation
    gateway: 192.168.1.1
    subnet: 255.255.255.0
  # Enable fallback hotspot (captive portal) in case wifi connection fails
  ap:
    ssid: "Power-Sonoff-${sonoff_num} Fallback Hotspot"
    password: "XY1XjkmKS1od"

captive_portal:

uart:
  rx_pin: RX          # GPIO3 on the ESP8266
  baud_rate: 4800
  #data_bits: 8
  parity: EVEN        # required by CSE7766
  #stop_bits: 1

#binary_sensor:
#  - platform: gpio
#    pin:
#      number: GPIO0
#      mode: INPUT_PULLUP
#      inverted: True
#    name: "Button Sonoff ${sonoff_num}"
#    on_press:
#      - switch.toggle: relay
#  - platform: status
#    name: "Status Sonoff ${sonoff_num}"

sensor:
#  - platform: wifi_signal
#    name: "Sonoff S31 WiFi Signal"
#    update_interval: 60s
  - platform: cse7766
    #update_interval: 30s
    energy:
      name: "Energy Sonoff ${sonoff_num}"
      accuracy_decimals: 1
    current:
      name: "Current Sonoff ${sonoff_num}"
      accuracy_decimals: 1
    voltage:
      name: "Voltage Sonoff ${sonoff_num}"
      accuracy_decimals: 1
    power:
      name: "Power Sonoff ${sonoff_num}"
      accuracy_decimals: 1
      id: my_power
  - platform: total_daily_energy
    name: "Daily Energy Sonoff ${sonoff_num}"
    accuracy_decimals: 1
    power_id: my_power

switch:
  - platform: gpio
    name: "Switch Sonoff ${sonoff_num}"
    pin: GPIO12
    id: relay
    restore_mode: RESTORE_DEFAULT_ON

time:
  - platform: sntp
    id: my_time

status_led:
  pin: GPIO13
    