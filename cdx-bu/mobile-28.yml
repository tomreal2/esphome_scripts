substitutions:
  mobilenum: "28"
  sensor_sda: "21"
  sensor_scl: "22"
  
esphome:
  name: mobile-${mobilenum}

esp32:
  board: nodemcu-32s
  framework:
    type: arduino

# Enable logging
logger:

# Enable Home Assistant API
api:
  encryption:
    key: "lS7/KaL6bTTYSDKDAKLSrvPZUbfMUdBQrCiSv8/E60U="

ota:
  password: "eca0ba4d24c961be40ecc49a5753b6af"

wifi:
  ssid: !secret wifi_ssid
  password: !secret wifi_password

  # Enable fallback hotspot (captive portal) in case wifi connection fails
  ap:
    ssid: "Mobile-${mobilenum} Fallback Hotspot"
    password: "TlxjNHlrGPem"

captive_portal:
    
i2c:
  - id: i2c_sensor_${mobilenum}
    sda: ${sensor_sda}
    scl: ${sensor_scl}
    scan: True
    
sensor:
  - platform: sht3xd
    i2c_id: i2c_sensor_${mobilenum}
    temperature:
      id: "idtemperature_${mobilenum}"
      name: "Temperature${mobilenum}"
    humidity:
      id: "idhumidity_${mobilenum}"
      name: "Humidity${mobilenum}"
      accuracy_decimals: 1
    update_interval: 3s